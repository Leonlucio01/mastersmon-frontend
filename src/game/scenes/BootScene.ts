import { BaseScene } from './BaseScene';

export class BootScene extends BaseScene {
  constructor() {
    super('BootScene', 'Boot', 'Configurando el juego y cargando estado local', 0x5da9ff);
  }
}
