import { BaseScene } from './BaseScene';

export class PreloadScene extends BaseScene {
  constructor() {
    super('PreloadScene', 'Preload', 'Preparando escenas, overlays y assets remotos', 0x6ec1ff);
  }
}
