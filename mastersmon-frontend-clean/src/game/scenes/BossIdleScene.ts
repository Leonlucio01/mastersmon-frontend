import { BaseScene } from './BaseScene';

export class BossIdleScene extends BaseScene {
  constructor() {
    super('BossIdleScene', 'Boss / Idle', 'Evento diario y auto-farm con hora Lima', 0xbd6dff);
  }
}
